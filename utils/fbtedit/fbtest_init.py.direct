import os
from fbtest import Test, TestVersion, Result, Resource, Repository, Runner

repository = Repository(os.getcwd())
repository.load()
runner = Runner(repository)
runner.set_target("XX","localhost")

