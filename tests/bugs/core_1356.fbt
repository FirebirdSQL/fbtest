{
'id': 'bugs.core_1356',
'qmid': None,
'tracker_id': 'CORE-1356',
'title': 'TYPE OF COLUMN in PSQL',
'description': '',
'min_versions': '2.5.0',
'versions': [
{
 'firebird_version': '2.5.0',
 'platform': 'All',
 'connection_character_set': 'UTF8',
 'page_size': '4096',
 'init_script': """CREATE TABLE PERSON (
  ID INTEGER,
  NAME VARCHAR(40)
  );
commit;
""",
 'test_type': 'ISQL',
 'test_script': """set term !!;
CREATE PROCEDURE SP_INS_PERSON (
  ID TYPE OF COLUMN PERSON.ID,
  NAME TYPE OF COLUMN PERSON.NAME
  )
  AS
DECLARE VARIABLE NEW_ID TYPE OF COLUMN PERSON.ID;
BEGIN
  INSERT INTO PERSON (ID, NAME)
    VALUES (:ID, :NAME)
    RETURNING ID INTO :NEW_ID;
END!!
set term ;!!
commit;
show procedure SP_INS_PERSON;

""",
 'expected_stdout': """Database:  localhost:C:\fbtest2\tmp\bugs.core_1356.fdb, User: SYSDBA
SQL> SQL> CON> CON> CON> CON> CON> CON> CON> CON> CON> CON> SQL> SQL> SQL> Procedure text:
=============================================================================
DECLARE VARIABLE NEW_ID TYPE OF COLUMN PERSON.ID;
BEGIN
  INSERT INTO PERSON (ID, NAME)
    VALUES (:ID, :NAME)
    RETURNING ID INTO :NEW_ID;
END
=============================================================================
Parameters:
ID                                INPUT (TYPE OF COLUMN PERSON.ID) INTEGER
NAME                              INPUT (TYPE OF COLUMN PERSON.NAME) VARCHAR(40)
SQL> SQL>"""
}
]
}
